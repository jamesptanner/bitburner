import { unimplemented } from '/contracts/solveContract';
// "Find Largest Prime Factor"

// Given a number, find its largest prime factor. A prime factor
// is a factor that is a prime number.
export function largestPrimeFactor(data:any):number|string[]|undefined{return unimplemented(data)}

// "Subarray with Maximum Sum"

// Given an array of integers, find the contiguous subarray (containing
// at least one number) which has the largest sum and return that sum.
export function MaxSubArray(data:any):number|string[]|undefined{return unimplemented(data)}

// "Total Ways to Sum"

// Given a number, how many different ways can that number be written as
// a sum of at least two positive integers?
export function TotalSums(data:any):number|string[]|undefined{return unimplemented(data)}