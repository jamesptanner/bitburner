import { NS } from '@ns'
import { initLogging } from '/shared/logging';

export const bootstrapPath ="/bootstrap.tsbootstrap.js";

export async function main(ns : NS) : Promise<void> {
   // initLogging(ns)
}